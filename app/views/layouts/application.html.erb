<!DOCTYPE html>
<html>
<head>
  <title>CheckMeOut360</title>
  <%= javascript_include_tag "http://maps.google.com/maps/api/js?key=AIzaSyBxYlcqNk_QrZHHuXLxLnqr65DGnDUgfpE&sensor=true" %>
  <%= stylesheet_link_tag    "application" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>

<body>

  <div class="navbar navbar-fixed-top">
    <div class="navbar-inner">
      <div class="container-fluid">
        <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </a>
        <a class="brand" href="#">CheckMeOut-360 <%= image_tag "globe.png", :size => "60x30", id: "main_globe_icon" %></a>
        <div class="nav-collapse">
          <ul class="nav">
            <li class="active"><%= link_to "Home", home_index_path %></li>
            <li><a href="#about">About</a></li>
            <li><a href="#contact">Contact</a></li>
          </ul>
          <p id="login_logout_top_menu" class="navbar-text pull-right">
            <% if current_user %>
              <%= link_to "Logout of #{current_user.profile.first_name}", destroy_user_session_path, :method => :delete %>
            <% else %>
              <%= link_to "Login Using Facebook", user_omniauth_authorize_path(:facebook), class: "login_with_facebook" %>
            <% end%>
            &nbsp;
            <% if current_user %>
              <%= image_tag display_profile_picture(current_user.profile.profile_picture), :id => "facebook_pic", :size => "30x30" %>
            <% else %>
              <%= link_to (image_tag "fb_login_icon.png", :id => "facebook_icon", class: "login_with_facebook", :size => "20x20"), user_omniauth_authorize_path(:facebook) %>
            <% end %>
            </p>
          
        </div><!--/.nav-collapse -->
      </div>
    </div>
  </div>
  <div class="container-fluid content">
    <div class="page-header">
      <div id="flash"></div> 
      <center><h1 id="main_page_header">CheckMeOut-360 <small>by Quy Phan and Phu Phan</small></h1></center>
    </div>
    
    <div class="row-fluid">
      <div class="span3">
        <div class="well sidebar-nav">
          <ul class="nav nav-list">
            <li class="nav-header">Active Users</li>
            <% User.active.each do |user| %>
              <li><%= link_to user.profile.full_name, profile_path(user.id) %></li>
            <% end %>
            <li class="nav-header">Inactive Users</li>
            <% User.inactive.each do |user| %>
              <li><%= link_to user.profile.full_name, profile_path(user.id) %></li>
            <% end %>
          </ul>
        </div><!--/.well -->
      </div><!--/span-->
      <div id="yield_for_main_content" class="span8">
        <%= yield %> <!-- !!! This is where all the contents of the views will render !!! -->
      </div><!--/span-->
    </div><!--/row-->

    <hr>

    <footer id="main_footer">
      <p>&copy; Quy Phan and Phu Phan</p>
    </footer>

  </div><!--/.fluid-container-->
  </body>
  
</html>
